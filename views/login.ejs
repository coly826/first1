<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Connexion</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">
  <style>
    body {
      background: linear-gradient(135deg, #d9afd9 0%, #97d9e1 100%);
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
    }

    .login-container {
      background: #fff;
      padding: 30px;
      border-radius: 15px;
      box-shadow: 0 8px 16px rgba(0, 0, 0, 0.1);
      width: 100%;
      max-width: 400px;
      animation: fadeIn 1s ease;
    }

    .input-group {
      position: relative;
    }

    .form-icon {
      position: absolute;
      left: 10px;
      top: 50%;
      transform: translateY(-50%);
      color: #6c757d;
    }

    .input-group .form-control {
      padding-left: 35px;
    }

    .form-control:focus {
      box-shadow: 0 0 10px rgba(13, 110, 253, 0.5);
      border-color: #0d6efd;
      transition: 0.3s;
    }

    .btn-primary, .btn-link {
      width: 100%;
      transition: 0.3s ease;
    }

    .btn-primary:hover {
      background-color: #0b5ed7;
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(-20px); }
      to { opacity: 1; transform: translateY(0); }
    }
  </style>
</head>
<body>
  <div class="login-container">
    <h5 class="text-center mb-4">Connecter-vous en tant que Admin</h5>
    <form method="POST" action="/login">
      <div class="input-group mb-3">
        <i class="bi bi-envelope-fill form-icon"></i>
        <input name="email" type="email" class="form-control" placeholder="Email" required>
      </div>
      <div class="input-group mb-4">
        <i class="bi bi-lock-fill form-icon"></i>
        <input name="password" type="password" class="form-control" placeholder="Mot de passe" required>
      </div>
      <div class="input-group mb-3">
        <input class="form-check-input" type="checkbox" name="terms" id="terms" required>
      <label class="form-check-label" for="terms">
        &ensp;Garder-moi Connecté
    </label>
      <button type="submit" class="btn btn-primary mt-2">Se connecter</button>
    </form>
    <a href="/register" class="btn btn-link text-center" style="text-decoration: none;">Pas encore inscrit ? <span style="color: orange;">Créer un compte</span></a>
    <a href="/forgot-password" class="btn btn-link text-center">Mot de passe oublié ?</a>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
